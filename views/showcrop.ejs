<% layout("layouts/boilerplate") %>
    <div class="container mt-5">
        <h1 class="mb-4">Crop Details</h1>
        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= crop.name %></h5>
                <p class="card-text"><strong>pH:</strong> <%= crop.ph %></p>
                <p class="card-text"><strong>EC:</strong> <%= crop.ec %></p>
                <p class="card-text"><strong>Temperature:</strong> <%= crop.temperature %> °C</p>
                <a href="/farm" class="btn btn-primary">Back to Home</a>
            </div>
        </div>
    </div>

    <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Crop Records</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            color: #333;
        }
        h2 {
            text-align: center;
            color: #006400;
            margin: 20px 0;
        }
        table {
            border-collapse: collapse;
            width: 90%;
            margin: 20px auto;
            background-color: #fff;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: center;
        }
        th {
            background-color: #28a745;
            color: #fff;
            font-weight: bold;
        }
        td {
            font-size: 14px;
        }
        img {
            max-width: 80px;
            height: auto;
            border-radius: 5px;
        }
        .action-buttons {
            display: flex;
            justify-content: center;
            gap: 10px;
        }
        .btn {
            padding: 6px 12px;
            text-decoration: none;
            color: #fff;
            border-radius: 4px;
            font-size: 14px;
            transition: background-color 0.3s;
        }
        .btn-edit {
            background-color: #007bff;
        }
        .btn-edit:hover {
            background-color: #0056b3;
        }
        .btn-delete {
            background-color: #dc3545;
        }
        .btn-delete:hover {
            background-color: #c82333;
        }
        a {
            display: block;
            text-align: center;
            margin: 20px auto;
            text-decoration: none;
            color: #28a745;
            font-weight: bold;
            font-size: 16px;
        }
        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h2>All Crop Records</h2>
<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Plant Name</th>
            <th>Batch Number</th>
            <th>Date & Time</th>
            <th>pH Value</th>
            <th>Electrical Conductivity (EC)</th>
            <th>Temperature (°C)</th>
            <th>Plant Photo</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <% reading.forEach(record => { %>
        <tr>
            <td><%= record._id || 'No ID' %></td> <!-- Added fallback for ID -->
            <td><%= record.name || 'No Name' %></td> <!-- Added fallback for name -->
            <td><%= record.batch_no || 'No Batch' %></td> <!-- Added fallback for batch_no -->
            <td><%= record.datetime || 'No Date' %></td> <!-- Added fallback for datetime -->
            <td><%= record.ph || 'No pH Value' %></td> <!-- Added fallback for ph -->
            <td><%= record.ec || 'No EC Value' %></td> <!-- Added fallback for ec -->
            <td><%= record.temperature || 'No Temp' %></td> <!-- Added fallback for temperature -->
            <% if (record.photo) { %>
                <td><img src="/uploads/<%= record.photo %>" alt="Plant Photo"></td>
            <% } else { %>
                <td>No Photo</td>
            <% } %>
            <td class="action-buttons">
                <a href="/farm/editreading/<%= record.id || record._id %>" class="btn btn-edit">Edit</a>
                <form action="/farm/deletereading/<%= record.id || record._id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this record?');">Delete</button>
                </form>
            </td>
        </tr>
        <% }) %>
    </tbody>
</table>
<a href="/farm">Back to Home</a>
</body>
</html>




































    <!-- <h2>All Crop Records</h2>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Plant Name</th>
                <th>Batch Number</th>
                <th>Date & Time</th>
                <th>pH Value</th>
                <th>Electrical Conductivity (EC)</th>
                <th>Temperature (°C)</th>
                <th>Plant Photo</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <% reading.forEach(record => { %>
            <tr>
                <td><%= record.id %></td>
                <td><%= record.name %></td>
                <td><%= record.batch_no %></td>
                <td><%= record.datetime %></td>
                <td><%= record.ph %></td>
                <td><%= record.ec %></td>
                <td><%= record.temperature %></td>
                <% if (record.photo) { %>
                    <td><img src="/uploads/<%= record.photo %>" alt="Plant Photo"></td>
                <% } else { %>
                    <td>No Photo</td>
                <% } %>
                <td class="action-buttons">
                    <a href="/farm/editreading/<%=record.id %>" class="btn btn-edit">Edit</a>
                    <form action="/farm/delete/<%=record.id %>" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this record?');">Delete</button>
                    </form>
                </td>
            </tr>
            <% }) %>
        </tbody>
    </table>
    <a href="/farm">Back to Home</a>
</body>
</html>





 -->
